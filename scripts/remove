#!/bin/bash

source _common.sh
source /usr/share/yunohost/helpers

#=================================================
# REMOVE NODE CONFIGURATION
#=================================================

$garage layout remove "$secret_node_id"

garage_layout_apply
if [ $? -ne 0 ]
then
	ynh_print_warn "Unable to remove the node. Maybe the number of node staying alive is not enough"
fi

#=================================================
# REMOVE SYSTEM CONFIGURATIONS
#=================================================
ynh_script_progression "Removing system configurations related to $app..."

if ynh_hide_warnings yunohost service status $app >/dev/null; then
	yunohost service remove $app
fi

# Remove the dedicated systemd config
ynh_config_remove_systemd
sleep 10
if [[ "$data" != "no" ]] && [[ "$data" != "manual" ]]
then
	yunohost service stop "home-yunohost.app-$app-data.mount"
	sleep 5
	ynh_print_warn "unmounting Data partition"
	if ynh_hide_warnings yunohost service status "home-yunohost.app-$app-data.mount" >/dev/null; then
		yunohost service remove "home-yunohost.app-$app-data.mount"
	fi
	# if $purge # empty mounted folder before unmounting
	# then
	# 	ynh_safe_rm "$data_dir/data/*"
	# fi
	if ynh_config_remove_systemd --service="home-yunohost.app-$app-data"; then
		ynh_print_warn "Data partition unmount successfull"
	else
		ynh_print_warn "Data unmount failed, please stop and disable home-yunohost.app-$app-data.mount and `rm -r /home/yunohost.app/garage`"
	fi
	sleep 5
fi
if [[ "$metadata" != "no" ]] && [[ "$metadata" != "manual" ]]
then
	yunohost service stop "home-yunohost.app-$app-metadata.mount"
	sleep 5
	if ynh_hide_warnings yunohost service status "home-yunohost.app-$app-metadata.mount" >/dev/null; then
		yunohost service remove "home-yunohost.app-$app-metadata.mount"
	fi
	# Remove Metadata even if remove --purge is not selected
	ynh_safe_rm "$data_dir/metadata/*"
	if ynh_config_remove_systemd --service="home-yunohost.app-$app-metadata"; then
		ynh_print_warn "Metadata partition unmount successfull"
	fi
	sleep 5
	ynh_safe_rm "$data_dir/metadata"
fi

ynh_config_remove_logrotate

ynh_config_remove_nginx

#add wildcard subdomain
ynh_replace --match="server_name $domain" --replace="server_name $domain *.$domain" --file="/etc/nginx/conf.d/$domain.conf"
ynh_store_file_checksum "/etc/nginx/conf.d/$domain.conf"

ynh_safe_rm "/usr/share/yunohost/hooks/conf_regen/98-nginx_$app"

yunohost tools regen-conf nginx

#=================================================
# END OF SCRIPT
#=================================================

ynh_script_progression "Removal of $app completed"
